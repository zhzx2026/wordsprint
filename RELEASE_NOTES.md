刷单词 v2.1

这一版把「12 项需求」一次做齐，装机后请逐条试（尤其手感与手势）：

① 词库新增大学四六级三本：四级真题核心词 1162 · 四级英语词汇 3739 · 六级英语词汇 2078，
   全部带音标，主页筛选条新增「大学」一档。
② 文字显示：内置 Poppins/Quicksand 两套字体（字母 a 是单层写法，不是衬线花体），
   字号「标准 / 自动 / 特大」三档，自动档会按屏幕短边放大 —— 平板和大屏手机不再是小字。
③ 主题配色：5 套配色（暖纸 / 松林 / 落日 / 莓红 / 深海）× 跟随系统 / 浅色 / 深色，设置里实时切换。
④ 首页新增学习热力图：一格一天、近一年，颜色深浅=当天刷词量，点格子看当天明细。
⑤ 每日学习指标：每天目标可设（默认 50/100/150/200，也能自定义），达标打勾、未达标不打勾；
   弹窗里可以「只改今天」或「设为默认」。
⑥ 打卡统计：当前连续打卡 + 历史最高连续，都在首页顶部与战绩图里。
⑦ 战绩分享：一键生成竖版战绩图（今日目标完成情况 + 热力图 + 二维码），可保存到相册或直接分享；
   二维码指向仓库里的在线战绩页（jsDelivr 加速、微信内直接打开，**不会触发文件下载**）。
⑧ 多用户档案：首次使用起个名字；多个档案各自独立的学习进度、错词、收藏、热力图，
   可随时新增 / 切换 / 改名 / 删除（升级安装时旧进度自动归第一个档案，不丢）。
⑨ 二维码词本配置：可把「我的词本 + 每组词数 + 顺序 + 回炉节奏 + 每日目标」生成配置码，
   扫码或粘贴导入时自己选「替换原有词本」或「合并到现有词本」（只改配置，不动学习进度）。
⑩ 手势操作：刷词页支持上下左右四向手势 + 点按 + 长按（当前映射：上滑收藏 / 下滑看释义 /
   左滑不认识 / 右滑记住了 / 点一下翻面再点朗读 / 长按查词详情）—— 这版先按这套跑，
   装机试过手感后你定最终映射，我改代码时同步改设置页的手势说明。
⑪ 收藏：刷词页爱心按钮（♡/♥）随手收藏，收藏页按词书分组，可「开始复习」直接刷收藏词。
⑫ 查词：首页与设置里都有入口，单词/中文/前缀都能查，点开是详情卡（音标、释义、出自哪本词书、
   收藏、朗读），刷词时长按单词也能直接查。

安装：覆盖安装即可，学习进度、打卡记录、收藏、词本设置都不丢（同签名）。
本包是**暂存测试包**：没打 tag、没发 Release，手机不会收到 OTA 提示。

本包修正（针对上一版测试包）：
· 修好「点开始刷词进不去」的崩溃：刷词页的位置标签（tvPos）漏了绑定，一进页面就空指针。
· 版本号纠正为 v2.1：上一包被我错编成了 3.1（stable 2.0 之后的新一轮 dev 应该是同一主版本的 2.1）；
  规则与脚本（version.sh / VERSIONING.md）已一并改对，code 22 仍大于上一包的 21，能正常提示更新。
