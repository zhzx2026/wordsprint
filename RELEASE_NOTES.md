刷单词 v7.0

从 v6.0 到现在攒了九轮迭代，这一版一起转正。挑你点过名的说：

① **外观切换立即生效**。设置里换配色 / 深浅色 / 字体 / 字号，返回任何页面马上是新样子，
   不用再杀 App 重开；点已经选中的项也不会白闪一下了。

② **测试包跟着分支走**。更新源只剩两档：stable / 分支。在「设置 → 关于与更新 → 更新源 →
   分支」里能直接选任意一条开发分支的测试包（清单是手机直连 GitHub 拉的，不再连不上）；
   装哪条分支的包就默认盯哪条，也可以手动锁定。「点了分支却还显示 stable」的老毛病修了。

③ **四处交互按你说的改了**：回炉间隔挪进「设置 → 学习」（3/5/8 张，全局生效，词本弹层里
   不再有）；词本弹层底部只剩「开始刷词」，订正去错题本页点「开始订正」；批量改进度四个
   动作（标记已掌握 / 取消已掌握 / 从这里继续刷 / 从这段重新刷），改完可以整体撤销；
   首页今日目标删掉「温习」，只剩刷词的勾。

④ **战绩分享更好看了**：二维码不再压字；今日目标卡里那排小块整排删掉；顶部标语改成
   「我的单词战绩」；扫码打开的网页顶部有「⬇ 下载 App」按钮，正式版和各条分支的测试包
   都有 APK 直链。

从 v6.0 ～ v6.9 任何版本覆盖安装都行，学习进度、错题本、设置全都不丢。
装好后在「设置 → 关于与更新」里看到「v7.0」，再换个配色看首页跟不跟着变，就都验完了。
