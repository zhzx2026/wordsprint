刷单词 v1.0.14

① 进度码导入这条路彻底重做：扫码页点「粘贴进度码」现在打开**独立粘贴页**（v1.0.13 之前是挂在相机窗口上的弹窗，一弹键盘就被系统重建，表现为"点了直接退出"）。
② 粘贴容错：`WPX1.` 前缀可缺、可被中文说明/引号/换行/全角空格/零宽字符包围；标准与 URL 两套 Base64 字母表自动判别（以能否解压为准，不再"能解但字节全错"）；复制被截断时，已经把记录写完整的那部分先合并进来。
③ 导入失败会在页面下方给出一行可复制的诊断（原文字符数 / 有效字符数 / 卡在哪一步 / 开头结尾片段）——把那一行发给开发者就能定位。
④ 更新弹窗修好：卡片圆角外不再露出一圈白角；下载进度改成渐变进度条，「取消下载」真的能取消并删掉半截安装包。
⑤ 导出页的文本码常驻可见、可长按全选，点复制会告诉你复制了多少字符；分享目标缺失不再崩。设置页去掉重复的粘贴入口，只留扫码页那一个。
⑥ 页面标题与设置页页脚显示版本号，方便确认手机上装的到底是哪一版。

覆盖安装即可，学习进度、词书、打卡记录都不丢。
