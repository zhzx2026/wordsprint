刷单词 v8.3

这一版两件事：**刷词中途意外退出不再重头开始**，以及**去掉刷词页那些弹窗 + 错题本筛选改版**。

⓪ **弹窗清理**（你说的「奇怪的弹窗」）：「接着上次那张继续」「已进错题本：订正要对 3 次」「又错一次：
   还得对 N 次」「连对 3 次已掌握」「已撤销」「本组进度已保存」全都不弹了——错的档位去错题本看 ★ 就行，
   撤销按钮没得撤时自己变淡。刷词页现在只剩翻卡和作答，安安静静。

⓪′ **错题本第一行 = 多列独立筛选**：两个列按钮「词本：不限 ▾」「星级：不限 ▾」。点哪一列就只弹
   **那一列**自己的下拉，候选项**勾选、可多选**（词本可以同时勾几本；星级 ★ / ★★ / ★★★ 任选几档），
   勾一下列表立刻跟着变，底部「不限」一键清空该列、「收起」关掉下拉（点外面也能关）。再点另一列弹
   另一个独立下拉，各列条件互不干扰、叠加生效；按钮上写着当前勾了什么（多于一个显示「xx +N」）。
   列表上方有一行当前筛选结果的计数；「清空已掌握」只在星级包含 ★★★ 或不限时出现。
   原来的图例行和横向滑的 chips 条删了。

以下是意外退出那件事：

① 你报的「刷词意外退出会重头开始」，根因是存盘时机：以前只在「整组打完」和正常退出时才记
   进度，闪退 / 后台被系统清掉 / 强行停止都走不到那一步，本组已经刷过的卡就全丢了。
   现在**每翻出一张卡就存一次盘**：刷到第几张、哪些词还没过、「不认识」的词排到第几张回炉、
   本组正确率和用时，全都实时落盘。怎么退出都行，重开这本书就接着那一张继续，
   顶部会提示「接着上次那张继续」。

② 该重来的时候照样重来：勾了「重刷整本」、在词表里批量改过进度、扫码 / 粘贴导入合并过，
   旧的半截现场都会作废，按新进度重新切组，不会拿旧队列跟新设置打架。错题本订正不受影响。

③ 已经「记住了」的词不会被问第二遍：哪怕是点完「记住了」还没来得及出下一张就闪退，
   重开也直接顺延到下一张。

从 v8.0 ～ v8.2 覆盖安装即可，学习进度、错题本、设置全都不丢。
验证：进任意词书刷几张（中间点几次「不认识」）→ 从最近任务里划掉 App（或设置里「强行停止」）
→ 重新打开这本书：应该停在刚才那张卡，右上角计数 x / N 与退出前一致。
