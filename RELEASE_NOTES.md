刷单词 v6.1

本版修一件事：**外观切换的「及时生效」**（你报的「风格的切换没有及时更新」）。

以前在「设置 → 外观」里换配色 / 深浅色 / 字体 / 字号，只有设置页自己当场换装；
一路返回时，设置首页、App 首页这些「早就开着的页面」还是旧颜色，
得把 App 彻底杀掉重开才换过来 —— 看起来就像切换没生效。

现在换了一套机制（外观「代数」对账，`Look.java`）：
每改一次外观，代数 +1；每个页面回到前台时对一下自己出生时的代数，
对不上就自动按新风格重建。也就是说：**你翻回哪一页，哪一页就已是新配色**，
不用重启 App；深浅色（含跟随系统那档）、字体、字号也一并如此。

顺手的小修补：在设置里点「本来就是当前项」的字体 / 字号 chip，以前会把页面
白白重建一次（闪一下），现在没变化就不折腾。

从 v5.0 / v6.0（或更早）覆盖安装即可，同签名，学习进度、打卡、错词、档案、词本设置全都不丢。
