刷单词 v9.0

这一版把 8.1 ～ 8.3 三轮一起转正，三件事：

① **刷词中途意外退出，不再从本组第一张重来。** 以前只在整组打完 / 正常退出时记进度，闪退、
   后台被系统清掉、强行停止都记不上。现在**每翻出一张卡就存一次盘**（刷到第几张、哪些词没过、
   「不认识」的词排到第几张回炉、本组正确率和用时），怎么退出都接着那一张继续。
   勾「重刷整本」、批量改进度、扫码/粘贴导入合并后旧现场自动作废；已答对的词不会被问第二遍。

② **刷词页的提示弹窗全删了**：「接着上次那张继续」「已进错题本：订正要对 3 次」「又错一次」
   「连对 3 次已掌握」「已撤销」「本组进度已保存」都不再弹，刷词页只剩翻卡和作答。

③ **错题本第一行改成多列独立筛选**：「词本：不限 ▾」「星级：不限 ▾」两个列按钮，点哪列只弹
   那一列自己的下拉，候选项**勾选、可多选**，勾了列表立刻变；底部「不限」清该列、「收起」关掉。
   两列条件互不干扰、叠加生效；按钮上写着当前勾了什么。原来的图例行和横滑 chips 条删了。

从 v8.0 覆盖安装即可，学习进度、错题本、设置全都不丢。
验证：刷几张（点几次「不认识」）→ 划掉 App → 重开这本书停在同一张；错题本点「词本」「星级」各弹各的。
