刷单词 v6.5

上一版我修了三处，但漏了最要命的一处：点「分支」时，App 把它在按钮排里的位置（第 2 个）当成通道编号存了进去 —— 而分支的编号是 2。结果就是你看到的：**点「分支」，它还是显示 stable**。v6.3/v6.4 都有这个错，v6.3 里分支等于完全不能用。

这一版把「第几枚按钮 ↔ 哪个通道」的换算修正并加了测试锁死：

· 点「分支」→ 状态行立刻显示「分支·<你的分支> 通道」，不再跳回 stable；
· 之前在 v6.3/v6.4 里误存成 stable 的手机，装上这版后重新点一次「分支」即可（旧脏值也会自动纠正）；
· 顺带确认：没显式选过的手机，装的是哪条分支的包就默认盯哪条。

装好后在「关于与更新」里点「分支」，状态行那行字就是验证：前缀必须是「分支·」。
