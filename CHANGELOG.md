# CHANGELOG —— 历史发布文案存档

> 这里存的是**历代发布文案原文**（v4.0 及更早），只供查阅，**不会再进任何发布物**。
> 现役文案在 [RELEASE_NOTES.md](RELEASE_NOTES.md)：那个文件只写**当前这一版**的说明，
> 因为 `scripts/make_release_manifest.sh` 会把它整份塞进 Release 正文与 `update.json` 的 `notes`
> —— 而 `notes` 是手机「发现新版本」弹窗里直接显示的那段字。
>
> 教训（v4.0，2026-09-17）：文案从 2.x 一路堆到 4.0，202 行 / 20,292 字节的 `update.json`，
> 弹窗里是一整屏根本读不完的流水账。v5.0 起规矩改成：**发版前把上一版文案挪进本文件，
> `RELEASE_NOTES.md` 只留这一版**（规则写在 VERSIONING.md §6）。

---

## v8.0（2026-09-24 · stable 转正，code 57）

刷单词 v8.0

这一版专收拾「导出二维码」——传的东西不全、还没得选，两个毛病一起治。

① **传的东西补全了**。以前只传掌握进度 + 组指针 + 每日打卡数，换机后错题本、
    温习记录、每天的目标和用时全丢。现在一枚码里有：词书进度、**错题本**（含每词
    还差几次）、**完整打卡日记**（目标/温习/用时/完成勾）、**学习设置**
    （手势/每日目标/每组词数/回炉间隔/各书分组），还附一句“来自谁”。
    新码在旧版本 App 上照样能扫（旧版只读它认得的前半段，不报错）。

② **导出内容可以选了**。导出页顶部四枚开关：词书进度 / 错题本 / 打卡记录 /
    学习设置，想带什么点什么；下面一行「词书：已选 N/M 本」能逐本勾选，
    改完二维码和文本码当场重算。哪类没内容，哪枚开关自动置灰。

③ **内容太多不再闪退**。二维码有 2331 字节上限，重度用户（全词书 + 一整年打卡）
    以前点导出直接崩。现在装不下就明说：码图区换成提示，文本码照样可复制、
    可分享（粘贴导入不限长度），或少选几本分多次传。

④ **导入成功页说得更细**：合入几本、新增几词、几天打卡、错题本新增几个，
    外加“来自谁”。对方设置跟你的不一样会单出一行，可一键采用——
    **默认不动你的任何设置**，扫了别人的码也不会把手势悄悄换掉。

从 v7.0 覆盖安装即可，进度、错题本、设置全都不丢。
装好后去「设置 → 数据与档案 → 导出二维码」看一眼：四枚开关都在，统计行写着
几本几词几天，就是这一版。

---

## v7.1（2026-09-24 · dev：进度码传全+可选，code 55）

刷单词 v7.1

这一版专收拾「导出二维码」——按你说的两条：传的东西不全、还没得选。

① **传的东西补全了**。以前只传掌握进度 + 组指针 + 每日打卡数，换机后错题本、
    温习记录、每天的目标和用时全丢。现在一枚码里有：词书进度、**错题本**（含每词
    还差几次）、**完整打卡日记**（目标/温习/用时/完成勾）、**学习设置**
    （手势/每日目标/每组词数/回炉间隔/各书分组），还附一句“来自谁”。
    新码在旧版本 App 上照样能扫（旧版只读它认得的前半段，不报错）。

② **导出内容可以选了**。导出页顶部四枚开关：词书进度 / 错题本 / 打卡记录 /
    学习设置，想带什么点什么；下面一行「词书：已选 N/M 本」能逐本勾选，
    改完二维码和文本码当场重算。哪类没内容，哪枚开关自动置灰。

③ **内容太多不再闪退**。二维码有 2331 字节上限，重度用户（全词书 + 一整年打卡）
    以前点导出直接崩。现在装不下就明说：码图区换成提示，文本码照样可复制、
    可分享（粘贴导入不限长度），或少选几本分多次传。

④ **导入成功页说得更细**：合入几本、新增几词、几天打卡、错题本新增几个，
    外加“来自谁”。对方设置跟你的不一样会单出一行，可一键采用——
    **默认不动你的任何设置**，扫了别人的码也不会把手势悄悄换掉。

从 v7.0 覆盖安装即可，进度、错题本、设置全都不丢。
验法：导出页把四枚开关挨个关一遍，看统计行和二维码跟着变；
再开一枚“只传设置”的码扫进另一台手机，成功页应出现“采用对方设置”。

---

## v7.0（2026-09-24 · stable 转正：九轮迭代合版，code 54）

刷单词 v7.0

从 v6.0 到现在攒了九轮迭代，这一版一起转正。挑你点过名的说：

① **外观切换立即生效**。设置里换配色 / 深浅色 / 字体 / 字号，返回任何页面马上是新样子，
   不用再杀 App 重开；点已经选中的项也不会白闪一下了。

② **测试包跟着分支走**。更新源只剩两档：stable / 分支。在「设置 → 关于与更新 → 更新源 →
   分支」里能直接选任意一条开发分支的测试包（清单是手机直连 GitHub 拉的，不再连不上）；
   装哪条分支的包就默认盯哪条，也可以手动锁定。「点了分支却还显示 stable」的老毛病修了。

③ **四处交互按你说的改了**：回炉间隔挪进「设置 → 学习」（3/5/8 张，全局生效，词本弹层里
   不再有）；词本弹层底部只剩「开始刷词」，订正去错题本页点「开始订正」；批量改进度四个
   动作（标记已掌握 / 取消已掌握 / 从这里继续刷 / 从这段重新刷），改完可以整体撤销；
   首页今日目标删掉「温习」，只剩刷词的勾。

④ **战绩分享更好看了**：二维码不再压字；今日目标卡里那排小块整排删掉；顶部标语改成
   「我的单词战绩」；扫码打开的网页顶部有「⬇ 下载 App」按钮，正式版和各条分支的测试包
   都有 APK 直链。

从 v6.0 ～ v6.9 任何版本覆盖安装都行，学习进度、错题本、设置全都不丢。
装好后在「设置 → 关于与更新」里看到「v7.0」，再换个配色看首页跟不跟着变，就都验完了。

---

## v6.9（2026-09-23 · 合并 arena/01a0cec1 外观立即生效，code 53）

刷单词 v6.9

这一版把另一条线上赶出来的修复合了进来，一起发：

① **外观切换立即生效**（新）。以前在设置里换配色 / 深浅色 / 字体 / 字号，只有设置页自己变，
   返回首页或别的页面还是旧样子，要彻底杀掉 App 重开才换过来。现在改完任何一项，
   **返回到任何页面都立刻是新样子**；顺手修了「点当前已选中的项会白闪一下」的小毛病。
   验证：设置 → 外观换个配色 → 按返回回首页，首页马上就是新配色。

② **战绩图重排**（上一版内容，一并带上）：
   · 目标卡里那排小块整排删了（删温习后只剩一枚刷词，孤零零没意思），
     卡里只剩「今日目标 + 已刷 X/Y 词 + 进度条 + 右上角的勾」；
   · 顶部标语改成「我的单词战绩」（原来那句「今天也把单词刷了」配的却是累计数字，读不通）；
   · 二维码不再压字；扫码打开的网页顶部有「下载 App」按钮。

从 v6.4 ～ v6.8 覆盖安装即可，学习进度、错题本、设置全都不丢。

---

## v6.8（2026-09-23 · 战绩图重排，code 52）

刷单词 v6.8

这一版还是收拾分享战绩图，按你说的重排了版面：

① **「今日目标」卡里那排小块整排删了**。原来下面有两枚：刷词、温习。删掉温习以后
   只剩一枚刷词，孤零零的没意思 —— 现在整排都不要了，卡片里只剩「今日目标 + 已刷 X/Y 词 +
   进度条 + 右上角的勾」，更清爽。空出来的地方匀给了上下几段之间的留白，整张图不那么挤。

② **顶上那句「今天也把单词刷了」删了**。那句话配着下面的大数字本来就不对 —— 数字是
   **累计**掌握的总数，不是今天的。现在换成「**我的单词战绩**」，跟「累计掌握 301」这样的
   数字放一起读得通。扫码打开的网页也同步换了这句、删了同一样的小块。

③ 上一版（v6.7）的两处修复都还在：二维码不再压字、网页有下载按钮。

从 v6.4～v6.7 覆盖安装即可，学习进度、错题本、设置全都不丢。

---

## v6.7（2026-09-23 · 二维码压字修复 + 官网下载链接，code 50）

刷单词 v6.7

这一版修你说的两件事：

① **分享战绩图：二维码和字不再重叠**。原来图片最下面那行小字（带着版本号和构建号）
   是居中排的，字太长，右半截直接压到了二维码上。现在那行改成**靠左**、并且换成
   短文案「刷单词 · 素纸背单词」，版本号不再上分享图（想看版本去 设置 → 关于与更新）。
   验证：App 里点分享，生成的图上二维码和文字中间留白清晰，互不搭边。

② **战绩网页有了下载链接**。以前那个网页只能看战绩，想装 App 得自己去翻 Releases。
   现在：
   · 页面顶部（深蓝卡片里）多了 **「⬇ 下载 App（正式版 APK）」** 大按钮，点了直接下正式版；
   · 下面「App 版本一览」里，正式版和每条开发分支的测试包都带 **⬇ APK** 直链；
   · 说明文字里还有「最新测试包」直链（想尝鲜的用这个，装了会自动更新）。
   验证：手机浏览器打开战绩分享页，点顶部下载按钮，能下载到 APK 安装包。

从 v6.4/v6.5/v6.6 覆盖安装即可，学习进度、错题本、设置全都不丢。

---

## v6.6（2026-09-23 · 交互四连改，code 49）

刷单词 v6.6

这一版按你说的改了四个地方：

① **回炉间隔挪进设置了**。打开词本后的弹层里不再有「回炉间隔」，它现在在
   **设置 → 学习 → 不认识 · 回炉间隔**（3/5/8 张，全局生效，所有词本一个标准）。

② **词本弹层里不再有「错词复习」**。要去错题本里点：打开**错题本**（词本弹层里那枚、
   或首页的错题本入口）→ 右上角**「开始订正」** → 选词本就开刷。词本弹层底部现在只有「开始刷词」。

③ **批量改进度变聪明了**。还是输「从哪到哪」，但动作从两个变四个：
   · **标记已掌握** —— 这段不刷了；
   · **取消已掌握** —— 这段恢复成没学过；
   · **从这里继续刷** —— 下次就从这段开头接着刷（已会的自动跳过，等于从不会的开始）；
   · **从这段重新刷** —— 下次从这段开头，并且整段清成没学过，从头再来。
   弹层里会先告诉你「这段里有几个已掌握、当前下次从第几个刷」，改完可以整体撤销。

④ **首页「今日目标」里的「温习」删掉了**。那行只剩「刷词」的勾，清爽了。

从 v6.4/v6.5 覆盖安装即可，学习进度、错题本、设置全都不丢。

---

## v6.5（2026-09-23 · 「分支」写入映射修复，code 48）

刷单词 v6.5

上一版我修了三处，但漏了最要命的一处：点「分支」时，App 把它在按钮排里的位置（第 2 个）当成通道编号存了进去 —— 而分支的编号是 2。结果就是你看到的：**点「分支」，它还是显示 stable**。v6.3/v6.4 都有这个错，v6.3 里分支等于完全不能用。

这一版把「第几枚按钮 ↔ 哪个通道」的换算修正并加了测试锁死：

· 点「分支」→ 状态行立刻显示「分支·<你的分支> 通道」，不再跳回 stable；
· 之前在 v6.3/v6.4 里误存成 stable 的手机，装上这版后重新点一次「分支」即可（旧脏值也会自动纠正）；
· 顺带确认：没显式选过的手机，装的是哪条分支的包就默认盯哪条。

装好后在「关于与更新」里点「分支」，状态行那行字就是验证：前缀必须是「分支·」。

---

## v6.4（2026-09-23 · 修清单/自动认领/高亮，未转正，code 47）

刷单词 v6.4

你指出「分支都没用，没反应」—— 查下来是我的锅，三处：

① **分支清单走错了域名**。之前它直连 api.github.com 拉分支列表，这个域名在手机网络下经常连不上
   （还有限流），拉不到清单，那一行就一直卡着 —— 看起来就是点了没反应。
   现在改从测试包同一个地方（github.com 的 ci 通道元数据）读清单，**能下包就一定能拉到清单**。
② **点了不亮**。选「分支」后高亮状态算错了，点完看不出任何变化，像没按到。已修。
③ **装了哪条分支的包还得再手点一次同名分支**，多余。现在装哪个分支的包，App 自动认领哪条
   （设置页里它已经亮着，直接点「检查」就行），想换测别的分支再手点。

进「关于与更新」→ 更新源点「分支」，正常会看到：你的分支已高亮 + 「检查」能查到这一版。
要是清单一时拉不到，会写明原因（连不上 / 点「刷新」重试），不再默默卡住。

---

## v6.3（2026-09-22 · 更新源收敛两档版，未转正，code 46）

刷单词 v6.3

你装上 v6.2 后指出：**「安装界面 dev 还在」**——上一版只删了服务器上的 dev 分支，App 更新源里还留着「dev」这个选项。这一版把它删干净：

· **更新源只剩两档：stable / 分支**。「dev」不再出现在 App 任何地方；
· 旧版里选过「dev」的手机会自动迁到「分支」档（存量通道号迁移，不用你动手）；
· 装的是测试包（版本号第二位不是 0）时，默认就盯「分支」档 —— 不再出现「明明有新包却显示已是最新」；
· 显式选了 stable 就只看正式版，App 不再替你偷看别的源；
· 服务器上保留的「最近一次构建」聚合资产还在（旧手机过渡用），但 App 里已经没有它的入口 ——
  按你说的：聚合不出现在任何最外面，测试包一律按分支走。

升级后第一次进「关于与更新」，在「分支」一栏点一下你自己的分支，以后这台手机就只认它。

---

## v6.2（2026-09-22 · 通道重做版，未转正，code 45；只在 ci 通道上活了几小时就被 v6.3 跟进修正）

刷单词 v6.2

这一版按你的话把测试通道整个重做了：**dev 聚合分支删掉，App 直连 GitHub，包跟着分支走。**

你指出的两点都对：dev 分支只是个「最近一次构建」的聚合，还常驻在分支列表最外面；之前第 3 档还得依赖它的坑位目录。现在：

· **dev 分支已删除**。分支列表从此只有 main + 各条工作分支，干干净净。
· **测试包改挂在一个预发布 Release「ci」上**（不是分支，不进分支列表；正式版的 releases/latest 永远跳过它）：
  根资产 = 最近一次构建；`update-<分支id>.json` / `wordsprint-<分支id>.apk` = 每条分支自己的包，互不覆盖。
· **App 的「分支」档直连 GitHub**：分支清单直接读 api.github.com 的 /branches（新分支推上去立刻能选），
  选中哪条就只盯哪条的 update.json；没出过包的分支会标「·无包」，点了也会明说，不再甩「HTTP 404」。
· **在线战绩页（Pages）切到 main 托管**，页脚新增「App 版本一览」：正式版版本号 + 每条开发分支的
  测试包版本、code、说明，全是从 GitHub 实时拉的，谁也不用再猜哪条分支出到几版了。
  Releases 里的 ci 预发布正文也会自动维护同一张「分支 × 版本」索引表。
· **每次更新必须写清楚内容**：发包脚本直接取 RELEASE_NOTES 正文当 update.json 的说明，
  正文少于 40 字直接拒绝发包 —— 以后手机弹窗里那段字，就是这一包真实改了什么。

⚠️ 装过 v6.1（code 44）的手机注意：那一版的 dev 源指向已删除的 dev 分支，会「检查失败」——
从 Actions artifact（wordsprint-staging-v6.2-arena01a0c983-r<run>）手动装一次 v6.2 即可，
之后应用内更新恢复正常。v6.0 及更早版本不受影响。

---

## v6.1（2026-09-22 · dev 通道包，未转正，code 44；dev 聚合分支当日退役，此版只在通道上活了几小时）

刷单词 v6.1

这一版只改一件事：**更新源从两档变三档**，多分支的测试包在手机上可以直接选了。

你之前的问题一直挂着：更新只有 stable / dev 两个选项 ——
stable 是正式版（只有转正发布才会动），dev 是「最近一次构建」：
几条开发分支同时跑的时候，谁后出包 dev 就变成谁的，你想单独测某条分支，没有入口。

现在第三档「分支」补上了：

· 打开 **设置 → 关于与更新 → 更新源**，选「分支」，下面会列出当前所有测试分支的坑位
  （比如 arena01a0b2c2、arena01a0c983），点一条就锁定它；
· 每条分支的坑位只有那条分支自己的构建会刷新，**别的分支再怎么出包都影响不到你**；
· 名单右边有「刷新」，随时重新拉取；状态一行会写明当前锁的是哪条分支
  （当前 v6.1（code 44）· 分支·arena01a0c983 通道）。

从 v6.0 覆盖安装即可，同签名，学习进度、打卡、错词、档案、词本设置全都不丢。

---

## v6.0（2026-09-21 转正发布，code 43；dev 5.1→6.0 未单独发包，随转正一起）

> 下面这份是 v6.0 发布时 `RELEASE_NOTES.md` 的原文，原样存档。

刷单词 v6.0

本版把两件事一起交付 —— 一件是你在手机上能直接感觉到的，一件是仓库里的。

① **更新时的进度条，这次真修好了**（你前后报了四次「更新没有进度条」）。
   以前它是「系统控件 + 一个 drawable」，主题解析不出来 / ROM 配色盖上去 / drawable 被换掉 / level 没刷新，
   这四条里任意一条出问题，结果都是「数字在跳、条子看不见」，而且在电脑上测不出来。
   现在整条自己画：两个圆角矩形，只要屏幕上有位置就一定画得出像素；
   颜色解析失败自动换成实色兜底；服务器不给文件大小时也不会卡在 0%；
   弹窗里明确走「连接中 → 下载中（百分比 / 已下 / 大小 / 速度）→ 正在校验安装包 → 准备安装」四个阶段；
   从「关于与更新」退回上一页也不会再把进度窗口弄丢了。
   另外加了一条主机测试，以后谁再把它改隐形，**CI 直接红**，不用等你装机才发现。

   ⚠️ 一个必须说清楚的前提：更新时显示进度条的是**你手机上当时那个版本**，不是新版本。
   所以从旧版更新到 v6.0 这一趟，看到的还是旧画法；等你装上 v6.0 **之后**的那次更新，才会见到新进度条。

② **仓库的「多会话并行」机制**（不影响你日常使用，但以后多线同时开发不再打架）：
   多个开发会话各占一条工作分支，版本号只在最后一次才“领号”，谁先合并谁先用、后到的自动避开；
   每次测试包都有自己的独立更新源坑位，装错包在设置页脚一眼就能看出分支与构建号。
   规则和分工写在仓库 `BRANCHING.md`。

从 v5.0 / v5.1（或更早）覆盖安装即可，同签名，学习进度、打卡、错词、档案、词本设置全都不丢。

## v5.1 → v6.0（dev 5.1 未单独发布，2026-09-21 随 v6.0 一起转正，code 42 → 43）

> v5.1 的文案当时写在 `RELEASE_NOTES.md` 里，但它先进了 main、没单独发版；
> v6.0 的发布文案改写成「给人看的两件事」版本（原文落在上方表格的下一节之前，此处保留 5.1 原文存档）。

刷单词 v5.1

本版只修一件事：**更新时的进度条**。你连着报了四次「更新没有进度条」
（弹窗圆角露白 → 弹窗被压到后面看不见 → 条根本画不出来 → 这次），前几次都只在
Android 那一侧改画法、改完只能等装机，所以来回了好几轮。这次换了做法。

① 进度条**不再用系统控件画**，改成自己一个像素一个像素画（`UpdateBar`）。
   以前的实现是「系统 ProgressBar + 一个 drawable」，靠 drawable 的 level 表示进度：
   主题属性解析不出来 → 颜色变透明；ROM 的强调色盖上去；drawable 被系统样式换掉；
   level 没刷新 —— 这四条里任何一条出问题，结果都是**「数字在跳、条子看不见」**，
   而且主机上根本测不出来。现在没有 drawable、没有 level、没有 tint，
   就是两个圆角矩形：底下灰轨道、上面品牌色进度，只要视图有尺寸就一定画得出像素。
② 配色不再依赖主题解析成功：alpha 不到 0xF0 的颜色一律判为「没解析出来」，
   换成不透明实色兜底。并且新增主机测试 `DlProgTest`，把 10 套配色（5 皮肤 × 浅/深）
   连「一个色都没解析出来」的兜底路径一起断言：三个色必须不透明、
   轨道和卡片的对比度 ≥ 1.18、进度和轨道的对比度 ≥ 1.75 —— 以后谁再把条子改隐形，
   **CI 直接红**，不用等你装机发现。
③ 百分比不会再卡在 0%：服务器不给文件长度时（分块传输），以前百分比一直算不出来、
   条子纹丝不动；现在按已下载量估算（最多显示 99%，校验完才拉满）。
   另外 1% 在 300px 宽的条子上只有 3px、被圆角一压就没了 —— 现在只要有进度，
   至少画一个「圆头」那么宽。
④ 弹窗里多了两个阶段，不会再「凭空消失」：连接中 → 下载中（百分比 + 已下/总量 + 速度）
   → **下载完成，正在校验安装包** → **校验通过，准备安装** → 才拉起系统安装器。
⑤ 修掉一条真的会让进度条彻底消失的路：进度弹窗是挂在**创建它那个页面**的窗口上的，
   你从「关于与更新」页退回设置列表页时，那个页面已经不在前面了，可代码判断用的
   `isShowing()` 依然返回 true —— 于是「下载在跑、屏幕上什么都没有」。
   现在判定加上了「弹窗挂在哪个页面」，页面换了就把弹窗重新挂到当前页面上。
⑥ 进度弹窗万一挂不上（页面正在销毁之类），不再静默：会明确告诉你
   「进度窗口没弹出来（原因），下载仍在后台继续」，装包照常进行；
   同时不会每 400ms 重试一次、攒一堆废弹窗。

从 v5.0（或更早）覆盖安装即可，同签名，学习进度、打卡、错词、档案、词本设置全都不丢。

⚠️ 一个必须说清楚的前提：**OTA 下载过程中显示的进度条，是你手机上「当前这个版本」画的**，
不是新版本画的。所以这一版修好的进度条，要等你装上 v5.1 **之后**的那次更新才看得到 ——
从 v5.0 更新到 v5.1 这一趟，用的还是 v5.0 里的旧画法。

## v5.0（2026-09-17 发布，code 41）

> 下面这份是 v5.0 发布时 `RELEASE_NOTES.md` 的原文，原样存档。

本版是**仓库整理版**：App 里的功能、界面、词库跟 v4.0 一模一样（一行代码都没改），
只把仓库本身收拾干净，并按你的要求把版本号定为 **5.0**。从 v4.0（或更早）覆盖安装即可，
同签名，学习进度、打卡、错词、档案、词本设置全都不丢。

装完能看出来的唯一变化：
· **「发现新版本」弹窗里的说明不再是一整屏流水账**。以前发布文案是把 `RELEASE_NOTES.md`
  整个文件塞进 `update.json` 的 `notes`，而那个文件从 2.x 一路堆到 4.0（202 行 / 20KB），
  弹窗里根本读不完。现在这个文件只写**当前这一版**，历史文案挪进仓库的 `CHANGELOG.md` 存档。

仓库侧整理（都不进 APK，装机的包内容不变）：
· CI：`staging.yml` 里「推送开发者通道」那一步重复了两遍 —— 每次构建都往 `dev` 分支推两次，
  顺带触发两次 Pages 部署（其中一次被 GitHub 取消）。删掉重复的那份。
· `README.md` 的数字对齐真实数据：**24 本词书 / 16,571 词**（原来写的是 21 本 9,592 词，
  漏了四六级三本）、`wdb.dat` 0.70MB（原写 0.40MB）、APK 约 0.9MB（原写 534KB）；
  v2~v4 那些从来没写进 README 的功能（两套字体、5 套配色、热力图、每日目标、多档案、
  错题本、战绩分享、自定义手势、查词、词表预览与批量改进度、撤销上一个）一并补齐。
· `test/` 分层：CI 真正跑的测试留在 `test/`，一次性调试脚本（QR 掩码排查那一批）收进
  `test/scratch/` 并写明怎么跑；删掉 `test/com/` 下 5 份过期的源码副本
  （那份 `Engine.java` 还停在没有「撤销」的版本，容易照着它看错逻辑）。
· 文档：`AGENT.md`（交接说明）与 `VERSIONING.md`（版本规则）里的「当前状态」和词库数字更新到 v5.0，
  踩坑清单的编号顺序理顺；新增 `scripts/README.md`（每个脚本干什么、什么时候用、哪几个已退役）。

---

## v4.0（2026-09-17 发布，code 40）

> 下面这份是 v4.0 发布时 `RELEASE_NOTES.md` 的原文。它本身就叠了 v2.x~v4.0 各批次的说明
> （「第十二批」「第十一批」…倒序往下），原样保留、不做改写。


> **版本编号说明**：本线编号从 2.x 整体改成 **3.x**，并按你的要求直接转正为 **4.0**
> （dev 3.1 → stable 4.0，2026-09-17）。
> 背景：9-16 有一个并行会话在**旧底子**上发布过 3.0（tag `v3.0` / code 38）——
> 那个包不含 2.9~2.14 与第十二批的任何功能，还把开发者通道刷成了它。
> 本版把**开发者通道与正式通道都拉回本线**：功能一律以本版为准，那个 3.0 请忽略。
> 从 v2.0 / v2.14 覆盖安装即可，学习进度、打卡、错词、档案都不丢。

本包修正（第十二批 · 装机反馈）：

① **更新进度条「又坏了」**——这次是条**根本画不出来**（弹窗在、百分数在跳，就是没有条）。
   根因找到了：进度条的样式写在 XML 里、颜色用的是主题属性（?attr/wpChipBg、?attr/wpBrand…），
   而它是用 Resources.getDrawable() 取的 —— 那条路径不带页面主题，主题属性解析不出来，
   轨道和进度全成了透明。现在整条直接在代码里用实色搭出来（同一套品牌色），
   任何配色 / 深浅模式下都看得见；顺带把条加高到 10dp，弹窗一出现就显示当前状态。
② 顺手把「下载完却装不上」也堵上：服务端给了长度就按字节数核对，再用 zip 读一次
   （里面必须有 AndroidManifest.xml），被截断的包、被塞进来的错误页一律判失败并**自动重下一次**；
   两次都不行才报错，并且把更新卡片重新亮出来（等于给一个「重试」按钮）。
   另外补掉两个「点了没反应」的坑：从设置页点更新、去授权页给完权限回来 ——
   以前只有首页会接着下载，现在三个入口都接；下载中切页面回来，进度弹窗会自动重新挂出来。
③ **热力图只看 6 个月**（3 个月 / 1 年两档删掉），整块（含左边星期标签）**居中**摆放 ——
   大屏上不再把一大半留白甩在右边；格子大小依旧按屏幕宽度自适应。
④ **错题本改版**：现在是**一个总错题本**（所有词书的错词同页按词书分段列出）；
   每个词本详情里新增「错题本」入口 —— 进去是同一个页面、筛选先落在那个词本上，点「全部」看总览。
   档位收成**三档**、用五角星代替文字：★ 未掌握 · ★★ 快掌握 · ★★★ 已掌握（星越多越熟）。
   规则按你的新说法：**进本就不出了** —— 连对 3 次算已掌握、仍留在本里，
   要清掉得手动删（行尾 ✕ 删一个，或一次「清空已掌握」）；已掌握的词不再进订正队列。

这一版把「12 项需求」一次做齐，装机后请逐条试（尤其手感与手势）：

① 词库新增大学四六级三本：四级真题核心词 1162 · 四级英语词汇 3739 · 六级英语词汇 2078，
   全部带音标，主页筛选条新增「大学」一档。
② 文字显示：内置 Poppins/Quicksand 两套字体（字母 a 是单层写法，不是衬线花体），
   字号「标准 / 自动 / 特大」三档，自动档会按屏幕短边放大 —— 平板和大屏手机不再是小字。
③ 主题配色：5 套配色（暖纸 / 松林 / 落日 / 莓红 / 深海）× 跟随系统 / 浅色 / 深色，设置里实时切换。
④ 首页新增学习热力图：一格一天、只显示**最近 6 个月**（整块居中，格子大小随屏幕自适应），
   颜色深浅=当天刷词量，点格子看当天明细。
⑤ 每日学习指标：每天目标可设（默认 50/100/150/200，也能自定义），达标打勾、未达标不打勾；
   弹窗里可以「只改今天」或「设为默认」。
⑥ 打卡统计：当前连续打卡 + 历史最高连续，都在首页顶部与战绩图里。
⑦ 战绩分享：一键生成竖版战绩图（今日目标完成情况 + 热力图 + 二维码），可保存到相册或直接分享；
   二维码指向仓库里的在线战绩页（jsDelivr 加速、微信内直接打开，**不会触发文件下载**）。
⑧ 多用户档案：首次使用起个名字；多个档案各自独立的学习进度、错词、热力图、手势与目标，
   可随时新增 / 切换 / 改名 / 删除（升级安装时旧进度自动归第一个档案，不丢）。
⑨ 二维码词本配置：可把「我的词本 + 每组词数 + 顺序 + 回炉节奏 + 每日目标」生成配置码，
   扫码或粘贴导入时自己选「替换原有词本」或「合并到现有词本」（只改配置，不动学习进度）。
⑩ 手势操作：刷词页支持上下左右四向手势 + 点按 + 长按，六个位置各绑什么**由你自己定**
   （设置 → 学习 → 手势操作），改完立刻生效（当前默认：上滑查词 / 下滑看释义 / 左滑不认识 /
   右滑记住了 / 点一下翻面 / 长按查词详情）。
⑪ 收藏：**已按你的要求删除**（v2.14）：爱心按钮、收藏页、手势里的收藏动作、战绩图里的收藏数
   一起删掉，不留半拉子入口。
⑫ 查词：首页与设置里都有入口，单词/中文/前缀都能查，点开是详情卡（音标、释义、出自哪本词书、朗读），
   刷词时长按单词也能直接查。

安装：覆盖安装即可，学习进度、打卡记录、词本设置都不丢（同签名）。
本包是**暂存测试包**：没打 tag、没发 Release，手机不会收到 OTA 提示。

本包修正（装机反馈）：
· 「字体每次点击都会变大一下」：根因是字号收口（Ui.finishSetup）被塞进了词书弹层的 refresh()，
  每点一次尺寸/顺序/回炉档位就按倍率再缩放一次 → 现在收口只在页面创建时做一次，
  缩放本身也改成幂等（反复调用只是把同一个结果再写一遍，不会再漂移）。
· 「点开始刷词进不去」的崩溃：刷词页的位置标签（tvPos）漏了绑定，一进页面就空指针 —— 已修。
· 版本号纠正：stable 2.0 之后的新一轮 dev 是同一主版本的 2.1（此前误编为 3.1），
  规则与脚本（version.sh / VERSIONING.md）已一并改对。

本包修正（装机反馈第二批）：
· 自测看中文想英文：以前进去只顶着一个「?」，中英文都看不见 —— 现在题面就是中文释义（大字），
  点一下翻面看英文单词 + 音标（还会朗读），中文挪到释义行对照。
· 错题本规则按你说的改：① 答错一次就进错题本（宁愿多收）；② 要**连续答对 3 次**才算已掌握
  （v2.15 起不再自动出本，见上文第十二批）；③ 订正期间再答错一次，还要多对一次（3→4→5…）。每次作答都会提示「还要对几次」，
  结算页的错词列表也标出每个词还差几次。老版本的错词会自动迁进来，不丢。
· 战绩图版面：修「最高连续 N 天」压在热力图星期标签（一/三/五/日）上的问题 ——
  版面尺寸全部改成按公式算 + 加了主机侧版面自检（网格不越界、那行字落在网格下方、二维码不出画布）。
· 字体「一次比一次大」：上一包已修（收口只做一次 + 缩放幂等），这包继续复核过没有遗留的重复缩放路径。

本包新增（装机反馈第二批）：
· 手势由你自己定：设置 → 手势操作，六个位置（上滑/下滑/左滑/右滑/点一下/长按）各挑一个动作
  （收藏 / 看释义 / 记住了 / 不认识 / 朗读 / 查词 / 跳过 / 不绑定），改完立刻生效，也能一键恢复默认。
  点按有个例外：没翻面时先翻面——不然谁都先想看眼答案。
· 用户名随时能改：设置页顶栏点自己的名字即可改；档案页里点自己那一行也能改（以前只能长按，藏太深）。
· 界面重排：首页与设置页的信息层级重做（分组卡片、统一留白与字号），不再一屏几种间距。

本包新增（第三批装机反馈）：
· 手势设置不再显示数字：上一包弹窗里把动作名当数字拼进了文字（显示成 1/2/3…），已修；
  现在每一项都是「记住了 / 看释义（翻面） / 收藏 / 不认识 / 朗读 / 查词 / 跳过 / 不绑定」。
· 战绩图「累计掌握 301」不再和右边的说明文字叠在一起（上一版先改字号后量宽度，量小了）。
· 战绩图四周留白：整张图上下左右都留出一样宽的白边（约 82px），顶部渐变从"出血"改成
  一张圆角卡片，卡片、热力图、二维码都不再贴边；画布顺势加高到 1080×1900。

本包新增（第四批装机反馈）：
· 战绩页不再「打开是源码」：二维码原来指向 jsDelivr，而 jsDelivr 官方就把 .html 当
  text/plain 发（浏览器只能显示源码，实测 W3C 校验器确认）。现在改挂 GitHub Pages，
  Content-Type 正确、参数原样保留、没有中转页 —— 扫码直接看到战绩页面。
· 战绩图二维码卡上那行「日期 · 我的词本 N 本」按你的要求去掉（只留日期）。
· 首页热力图能看到格子了：以前空格子用的是卡片同色，整张网格「一片白」；
  现在空格子是浅灰底，没有记录的日子也看得见格子，有记录的日子照旧四级绿色。
· 删除用户：档案列表每行右侧新增「删除」按钮（只剩一个档案时不显示），点一下确认即删；
  长按菜单里的删除也还在。
· 进首页就检查更新：原来「20 小时才查一次」→ 改成 30 分钟，并且每次回到首页都会查
  （发现新版直接弹提示，不用再去设置里手动点）。

本包新增（第五批装机反馈）：
· 点错了能退回来：刷词页卡片右上角多了一个「↩ 上一个」。按错了「记住了 / 不认识」，
  点它就把刚才那张卡原样摆回来（未翻面）重新选 —— 进度、错题本、今日新增、自测/温习
  计数都会一起回退，不会「点错了还把账算歪」。没得撤销时按钮是淡的。
· 手势动作精简：去掉了「跳过这个词」（按你要求），也去掉了「朗读」（翻面后再点一下本来
  就会朗读），选项从 8 个减到 6 个：不绑定 / 收藏 / 看释义 / 记住了 / 不认识 / 查词。
  老版本存过「跳过」的映射会自动退回默认值，不会出现「点了没反应」。
· 卡片上的快捷键也少了一个：去掉朗读按钮（♡ 收藏保留）；刷词页那行手势提示也缩短成
  「左滑 xx · 右滑 xx」两句，而且只在每次进来的第一张卡上出现一次。
· 爱心重画：以前是文字符号「♡ / ♥」（字形跟着系统字体走，粗细歪斜不受控，所以难看），
  现在是矢量心形（描边 / 实心两版严格同轮廓），刷词卡片和查词详情里都换了。

本包新增（第六批装机反馈）：
· 「我的词本」整块删掉（按你说的）：首页筛选条里的那一档、设置页入口、词书行标题上的 ★、
  「词本配置码(WPB1)」扫码/粘贴导入、以及背后的整个模块都删了。分享战绩二维码不受影响，
  照旧能扫。
· 更新进度条：以前进度只画在「下载中」那个弹窗里，弹窗被压到后面就什么也看不见。现在进度是
  全局的 —— 设置页更新卡片下方会一直挂着一条进度条 + 「12.3%  1.2 MB / 9.8 MB · 2.1 MB/s」，
  下载弹窗也会自己刷新，不会再出现「点了没反应」。
· 检查更新变灵敏：现在 App 在前台时每 60 秒静默查一次（以前是半小时），进首页立刻查一次；
  一发现新版，首页目标卡上方会亮出一条「发现新版本 vX.Y · 点这里更新」的横幅（点一下就更新），
  同一个版本只弹一次窗，但横幅一直在。
· 首页热力图：这次是真找到根因了 —— 空格子的颜色以前是「背景色混一点分隔线色」，暖纸风下
  分隔线(#E8E3D8)几乎和背景(#F6F3EC)一样浅，画在白卡片上等于没画。现在空档改成「卡片色混
  22% 正文灰」，并按同一套算法把 4 档颜色重排；新增主机测试拿 10 套配色（5 套皮肤 × 浅/深）
  逐一算对比度，低于阈值就直接测红，以后不会再「看不见格子」。
· 爱心重画（第二次）：这次直接换成 Material Design 官方的心形路径（实心 + 描边两版轮廓严格
  一致），不再是手搓的弧线。
· 词书「仅预览」+ 批量改进度：每一行词书右侧多了一个「预览」小按钮，点一下直接看整本词表；
  词书设置面板里也有「仅预览词表」和「批量改进度」两个按钮。
  - 仅预览：整本词表只读列出（词号 / 单词 / 音标 / 释义 / 已掌握打勾），可搜词或搜释义，
    不改进度、不进刷词。
  - 批量改进度：输入一个范围（写法很宽松：100-300 / 1~50 / 第100到300 / 只写一个数 /
    留空=整本 / 写反了也认 / 超出词表自动收敛），选「标记已掌握」或「取消已掌握」，
    一次改一批；改之前会显示「这段里现在有几个已掌握」，改完给一次整体撤销。
    范围解析写成纯 java 逻辑 + 主机测试（16 条断言），不会因为一个错字把进度改坏。

本包新增（第七批：更新检查查错了通道）：
· 根因找到了：App 的更新通道默认是「stable 正式版」，而正式版 Release 上最新的还是 v2.0（code 20），
  手机里装的却是 dev v2.9（code 30）—— 20 > 30 永远不成立，所以点「检查更新」只会说「已经是最新版本」。
  三处一起修：
  ① 装的是开发版包（版本号形如 2.9）→ 默认就盯 dev 通道，不再拿正式版的 2.0 去比；
  ② 就算你手动选了 stable，检查时也会顺带看一眼 dev：dev 上更新就用它，并在提示里写明
     「dev 通道的测试包」，从此不会再「明明有新包却说已是最新」；
  ③ 「已是最新版本」不再是干巴巴一句 —— 现在写成
     「已是最新版本（dev 通道 · 服务器 2.10 / code 31 · 本机 v2.9 / code 30）」，
     查了哪个通道、服务器什么版本、本机什么版本全摆出来，一眼能看出有没有查错。
· 这条判断写成了纯 java + 主机测试（VersTest，20 条断言）：谁再把「装 dev 包」默认成 stable 通道，CI 先红。

本包新增（第八批：热力图超出屏幕）：
· 根因：首页热力图原来是「固定 53 周 ≈ 907dp」塞在约 300dp 宽的卡片里，靠横向滚动看，
  右边那一截自然顶出屏幕。现在改成**按屏幕宽度自适应**：
    平板/大屏 → 铺满一年（53 周）；一般手机 → 用「能放下至少半年」的最大格子（格子更大更清楚），
    例如 6.1 寸屏是 9dp 格子 × 26 周，4.7 寸屏是 8dp 格子 × 27 周。
  整张网格永远落在卡片里，右边不会再切半格，也不需要横向滑动；横向滚动条那套代码一并删掉。
  小字（一/三/五/日、月份）加了字号下限 9.5dp，格子变小也不会看不清。
· 这套排布算术抽成纯 java（Heat.colsFor / Heat.chooseCell），主机测试逐个屏幕宽度 × 屏幕密度
  验证「网格宽度 ≤ 可用宽度、至少半年、格子不小于 6dp、平板铺满一年」——再不会有「右边超过屏幕」。

本包新增（第九批：进度条位置 + 热力图跨度）：
· 更新下载进度只放在**弹窗**里（按你说的）：设置页那条进度条去掉了。弹窗会一直显示
  「12.3%  1.2 MB / 9.8 MB · 2.1 MB/s」并自己刷新；下载中再点一次「立即更新」不会开第二条下载，
  而是把进度弹窗重新拉起来。
· 热力图可以自己选看多久了：卡片里多了一排「3 个月 / 6 个月 / 1 年」，选完记住（跟着档案存），
  默认 3 个月。实测 6.1 寸屏：3 个月 → 18dp 的大格子 13 周；6 个月 → 9dp 格子 26 周；
  1 年 → 6dp 格子 40 周（手机上放不下整天年，宁可格子小一点把能画的都画上，
  小字会自动写明「最近 9 个月」）。切换只重画出图和文案，不动任何学习数据。

本包修复（第十批：星期标签被切 + 多档案隔离）：
· 热力图左边「一 / 三 / 五 / 日」被切掉（你反馈的「3、6 月的周 1357 被遮住了」）：
  3 个月档格子放到 16~18dp 时，标签要占「偏移 1.7 格 + 一个字」≈39~43dp，
  而老代码只给它留了固定的 24dp → 四个标签整列被视图左边界切出去。
  现在标签预留宽度跟着格子一起算（Heat.layout 一体化排布），实测 6.1 寸屏
  3 个月 → 16dp 格子 + 39dp 标签位；6 个月 → 9dp 格子 + 25dp；一年 → 6dp + 24dp，都不再被切。
  上方「9月/10月」那行的高度也一起按格子算，格子大时不会顶到卡片边缘。
· 多用户档案隔离：查出来两处真问题，都修了 ——
  ① 切档案 / 删档案时先换了当前档案、再落盘日记，导致**上一个人的热力图/目标/连续天数被写进新档案**；
     现在顺序改成「先按旧档案落盘 → 切 → 只丢内存缓存」。
  ② 删掉「升级前那份老数据」那个档案时，只按 u0_ 前缀挑键 → 一条没删干净，
     还能被「新档案接管老进度」的逻辑捡回来；现在按 Profiles.ownedBy 精确判断归属：
     老数据档案删的是无前缀的学习数据键（b_/d_/s_/diary_v1/fav_v1/g_goal/g_ges/g_size/g_heat_span），
     主题/配色/字体/更新源/档案列表这些全局设置一条都不动，别人档案的数据也不会被连坐。
· 顺带修掉的两处自身错误：导出进度码与「继续上次的书」不再把**别的档案**的天数/书本算进来；
  战绩分享图上那句副标题以前会显示 "%1$s"（文案带参数后没同步），已恢复成正常句子。

本包新增（第十一批 · v2.14：设置分子页 · 删自测与收藏 · 词本详情按钮重排）：
· 设置改成像微信那样一层层进去：首页只有四行「外观 / 学习 / 数据与档案 / 关于与更新」，
  点进去才是子页面（深浅色·配色·字体·字号 | 语音·音标·音效·动画·每组词数·每日目标·手势 |
  档案切换·进度码导出与扫码导入·战绩分享 | 版本信息·更新通道·检查更新）。
  设置首页仍然只看得到「当前档案」，点名字直接改名。
· **自测**功能删除：首页那个「自测」习惯格、刷词结束页的「自测」按钮、看中文想英文的整套流程都去掉了；
  首页每日指标现在是两项（刷词目标 / 温习），战绩图与在线战绩页也同步去掉自测那一栏。
· **收藏**功能删除：卡片右上角爱心、收藏列表页、查词详情里的收藏按钮、手势里的「收藏」动作、
  战绩图与在线页里的收藏数全部移除。老数据里存过的「上滑=收藏」会被认成失效映射、自动退回默认
  （默认上滑改成查词），不会变成别的动作。
· 首页空出来的位置：习惯格变两格（刷词 / 温习），快捷入口第三个改成「**错题本**」——
  新加了一页错题本：按词书列出在册错词、每个词还差几次订正，点词看详情，右上角「开始订正」直接开刷。
· 词书详情（点词书弹出来的那一层）按钮重排：外面临时挂的「预览」按钮取消，只留里面的；
  里面改成「工具行三个等宽小按钮（仅预览词表 / 批量改进度 / 清除进度）」+ 底部「温习错词 + 开始刷词」
  并排（开始刷词更宽更显眼），组号信息挪进统计行，不再挤在按钮上。
